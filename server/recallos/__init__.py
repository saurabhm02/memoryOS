"""RecallOS — a competency-graph memory platform.

`recallos.core` is the platform: pure domain + application logic with no
dependency on any framework, database, or memory engine. Infrastructure lives in
`recallos.adapters`; concrete learning products live in `recallos.apps`.
"""
