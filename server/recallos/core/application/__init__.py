"""Application layer: use-cases and the ports (interfaces) they depend on. Use-cases
are added in the phase that introduces their first adapter; for Phase 0 only the
`OntologyRepository` port exists, because it already has a concrete implementation.
"""
