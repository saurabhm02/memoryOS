// Public entry point: the lazy (code-split) graph is the default export consumers use.
export { CompetencyGraph } from "./lazy";
export { type ConceptNodeData, type ConceptFlowNode } from "./ConceptNode";
